program name
var
	real r;
	integer i;
    boolean b;
begin
	r = .12;
	i = 1 + 2 * (3 - 4);
	i = 1 + 2 ^ (3+4) ^ 2 - 4/5 - 6*2;

	for(i = 1; i >= 10^2; i = i - 10) {
		r = r + 10. / 4. div 2. * 10.;
	}

	

	write(i, b);
end